import React from 'react'
// import noteContext from '../contex/noteContex' when using hook and context api 

const About = () => {
  // const a  = useContext(noteContext)
  // useEffect(() => {
  //   a.update()
  // }, [])
  // when using hook to update the state then use this 
  return (
    <div>
      This is about section
    </div>
  )
}

export default About
